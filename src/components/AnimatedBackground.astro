<link
  href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400;700&display=swap"
  rel="stylesheet"
/>

<div class="hero">
  <h1>Anthony Lorette</h1>
</div>

<style>
  .hero {
    position: relative;
    height: 100vh;
    width: 100vw;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    background: #1a1a2e;
    overflow: hidden;
    color: white;
    font-family: "Dancing Script", cursive;
    font-size: 3rem;
  }

  .hero h1 {
    position: relative;
    z-index: 2;
    font-size: 13rem;
    background: linear-gradient(
      90deg,
      rgba(0, 128, 255, 0.6),
      rgba(255, 0, 128, 0.6)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: text-glow 6s ease-in-out infinite;
  }

  .hero::before {
    content: "Anthony Lorette";
    position: absolute;
    top: 50%;
    left: -150%;
    width: 300%;
    height: 300%;
    background: radial-gradient(
      circle,
      rgba(0, 128, 255, 0.3) 0%,
      transparent 70%
    );
    filter: blur(120px);
    animation: light-sweep 6s linear infinite;
    font-size: 10vw;
    text-align: center;
    line-height: 300%;
    color: rgba(255, 255, 255, 0.1);
    white-space: nowrap;
  }

  .hero::after {
    content: "Anthony Lorette";
    position: absolute;
    top: 50%;
    left: -150%;
    width: 300%;
    height: 300%;
    background: radial-gradient(
      circle,
      rgba(255, 0, 128, 0.3) 0%,
      transparent 70%
    );
    filter: blur(120px);
    animation: light-sweep 8s linear infinite reverse;
    font-size: 10vw;
    text-align: center;
    line-height: 300%;
    color: rgba(255, 255, 255, 0.1);
    white-space: nowrap;
  }

  @keyframes light-sweep {
    0% {
      transform: translate(-100%, -50%);
    }
    50% {
      transform: translate(0, -50%) scale(1.1);
    }
    100% {
      transform: translate(100%, -50%);
    }
  }

  @keyframes text-glow {
    0%,
    100% {
      text-shadow:
        0 0 8px rgba(0, 128, 255, 0.8),
        0 0 16px rgba(255, 0, 128, 0.8);
    }
    50% {
      text-shadow:
        0 0 16px rgba(0, 128, 255, 1),
        0 0 32px rgba(255, 0, 128, 1);
    }
  }
</style>
